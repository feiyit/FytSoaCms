@page
@model FytSoa.Web.Pages.FytAdmin.Goods.BarCodeModifyModel
@{
    ViewData["Title"] = "条形码管理";
    Layout = "~/Pages/FytAdmin/_Layout.cshtml";
}
<style>.layui-form-select dl{max-height:180px;}</style>
<form class="layui-form form-cus" action="">
    <div class="layui-form-item">
        <div class="layui-inline">
            <label class="layui-form-label">品牌</label>
            <div class="layui-input-inline">
                <select name="BrankGuid" lay-verify="required" lay-search="" lay-verType="tips">
                    <option value="">请选择，支持搜索</option>
                    @{
                        foreach (var item in Model.codeList.Where(m => m.ParentGuid == "7b664e3e-f58a-4e66-8c0f-be1458541d14").OrderBy(m => m.Sort))
                        {
                            if (item.Guid == Model.skuModel.BrankGuid)
                            {
                                <option value="@item.Guid" selected="selected">@item.Name</option>
                            }
                            else
                            {
                                <option value="@item.Guid">@item.Name</option>
                            }
                        }
                    }
                </select>
            </div>
        </div>
        <div class="layui-inline">
            <label class="layui-form-label">年份</label>
            <div class="layui-input-inline">
                <select name="YearGuid" lay-verify="required" lay-search="" lay-verType="tips">
                    <option value="">请选择，支持搜索</option>
                    @{
                        foreach (var item in Model.codeList.Where(m => m.ParentGuid == "1942d4fd-3203-42b1-a955-4a84a532b2a2").OrderBy(m => m.Sort))
                        {
                            if (item.Guid == Model.skuModel.StyleGuid)
                            {
                                <option value="@item.Guid" selected="selected">@item.Name</option>
                            }
                            else
                            {
                                <option value="@item.Guid">@item.Name</option>
                            }
                        }
                    }
                </select>
            </div>
        </div>
    </div>
    <div class="layui-form-item">
        <div class="layui-inline">
            <label class="layui-form-label">季节</label>
            <div class="layui-input-inline">
                <select name="SeasonGuid" lay-verify="required" lay-search="" lay-verType="tips">
                    <option value="">请选择，支持搜索</option>
                    @{
                        foreach (var item in Model.codeList.Where(m => m.ParentGuid == "8cb134d5-979b-40e2-b453-aeee265f4ab2").OrderBy(m => m.Sort))
                        {
                            if (item.Guid == Model.skuModel.SeasonGuid)
                            {
                                <option value="@item.Guid" selected="selected">@item.Name</option>
                            }
                            else
                            {
                                <option value="@item.Guid">@item.Name</option>
                            }
                        }
                    }
                </select>
            </div>
        </div>
        <div class="layui-inline">
            <label class="layui-form-label">款式</label>
            <div class="layui-input-inline">
                <select name="StyleGuid" lay-verify="required" lay-search="" lay-verType="tips">
                    <option value="">请选择，支持搜索</option>
                    @{
                        foreach (var item in Model.codeList.Where(m => m.ParentGuid == "48458681-48b0-490a-a840-0ffcbe49f5d4").OrderBy(m => m.Sort))
                        {
                            if (item.Guid == Model.skuModel.StyleGuid)
                            {
                                <option value="@item.Guid" selected="selected">@item.Name</option>
                            }
                            else
                            {
                                <option value="@item.Guid">@item.Name</option>
                            }
                        }
                    }
                </select>
            </div>
        </div>
    </div>
    <div class="layui-form-item">
        <div class="layui-inline">
            <label class="layui-form-label">批次</label>
            <div class="layui-input-inline">
                <select name="BatchGuid" lay-verify="required" lay-search="" lay-verType="tips">
                    <option value="">请选择，支持搜索</option>
                    @{
                        foreach (var item in Model.codeList.Where(m => m.ParentGuid == "2e0393f9-e6d6-4c15-98cf-96072f0d3d70").OrderBy(m => m.Sort))
                        {
                            if (item.Guid == Model.skuModel.BatchGuid)
                            {
                                <option value="@item.Guid" selected="selected">@item.Name</option>
                            }
                            else
                            {
                                <option value="@item.Guid">@item.Name</option>
                            }
                        }
                    }
                </select>
            </div>
        </div>
        <div class="layui-inline">
            <label class="layui-form-label">尺码</label>
            <div class="layui-input-inline">
                <select name="SizeGuid" lay-verify="required" lay-search="" lay-verType="tips">
                    <option value="">请选择，支持搜索</option>
                    @{
                        foreach (var item in Model.codeList.Where(m => m.ParentGuid == "e86cf108-dc4d-4532-8cce-fdb041363902").OrderBy(m => m.Sort))
                        {
                            if (item.Guid == Model.skuModel.SizeGuid)
                            {
                                <option value="@item.Guid" selected="selected">@item.Name</option>
                            }
                            else
                            {
                                <option value="@item.Guid">@item.Name</option>
                            }
                        }
                    }
                </select>
            </div>
        </div>
    </div>
    <div class="layui-form-item">
        <div class="layui-inline">
            <label class="layui-form-label">销售价格</label>
            <div class="layui-input-inline">
                <input type="text" name="SalePrice" value="@Model.skuModel.SalePrice" autocomplete="off" class="layui-input" maxlength="4" lay-verify="required" lay-verType="tips">
            </div>
        </div>
        <div class="layui-inline">
            <label class="layui-form-label">打折价格</label>
            <div class="layui-input-inline">
                <input type="email" name="DisPrice" value="@Model.skuModel.DisPrice" autocomplete="off" class="layui-input" maxlength="4" lay-verify="required" lay-verType="tips">
            </div>
        </div>
    </div>
    <div class="layui-form-item">
        <div class="layui-input-block">
            <button class="layui-btn" lay-submit="" lay-filter="submit">立即提交</button>
            <button type="button" class="layui-btn layui-btn-primary btn-open-close">取消</button>
        </div>
    </div>
    <input type="hidden" name="Guid" id="Guid" value="@Model.skuModel.Guid" />
    <input type="hidden" name="GoodsGuid" value="@Model.skuModel.GoodsGuid" />
    <input type="hidden" name="AddDate" value="@Model.skuModel.AddDate" />
    <input type="hidden" name="SaleSum" value="@Model.skuModel.SaleSum" />
    <input type="hidden" name="StockSum" value="@Model.skuModel.StockSum" />
</form>
<link rel="stylesheet" href="~/themes/ztree/css/metroStyle/metroStyle.css" type="text/css">
@section Scripts{
    <script>
        layui.config({
            base: '/themes/js/modules/'
        }).use(['layer', 'jquery', 'common', 'form'], function () {
            var form = layui.form, $ = layui.$, os = layui.common;
            var index = parent.layer.getFrameIndex(window.name);
            //监听提交
            form.on('submit(submit)', function (data) {
                var urls = "api/goods/addbarcode";
                if ($("#Guid").val()) {
                    urls = "api/goods/editbarcode";
                }
                data.field.Status = data.field.Status === 'on' ? true : false;
                os.ajax(urls, data.field, function (res) {
                    if (res.statusCode == 200) {
                        parent.layer.close(index);
                    }
                    else {
                        os.error(res.message);
                    }
                });
                return false;
            });
            $(".btn-open-close").on('click', function () {
                parent.layer.close(index);
            });            
        });
    </script>
}


